<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/electric_blue</item>
        <item name="colorPrimaryVariant">@color/midnight_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/surface_primary</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.MyApp" parent="Base.Theme.MyApp" />

    <!-- Bottom Navigation Style -->
    <style name="Widget.MyApp.BottomNavigationView" parent="Widget.Material3.BottomNavigationView">
        <item name="itemIconTint">@drawable/bottom_nav_selector</item>
        <item name="itemTextColor">@drawable/bottom_nav_selector</item>
        <item name="android:background">@color/white</item>
        <item name="itemBackground">@android:color/transparent</item>
        <item name="elevation">0dp</item>
        <item name="android:selectableItemBackground">@android:color/transparent</item>
        <item name="selectableItemBackground">@android:color/transparent</item>
        <item name="itemRippleColor">@android:color/transparent</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:itemBackground">@android:color/transparent</item>
    </style>

    <!-- Bottom Navigation Text Styles -->
    <style name="TextAppearance.MyApp.BottomNavigationView" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textSize">12sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/selector_bottom_nav_text_color</item>
    </style>

    <style name="TextAppearance.MyApp.BottomNavigationView.Active" parent="TextAppearance.MyApp.BottomNavigationView">
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Custom Card Style -->
    <style name="CardView.Lifesaver" parent="CardView">
        <item name="cardCornerRadius">6dp</item>
        <item name="cardElevation">0dp</item>
        <item name="cardUseCompatPadding">false</item>
    </style>

</resources>